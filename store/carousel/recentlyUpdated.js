function recentlyUpdated() {
  // this section jsute disapeared for some reason ????
  // will do it when it'll be displayed again I guess....
  
  /*
  const itemContainer = document.querySelector('.recently_updated_apps > .carousel_items')
  const gameUrls = []
  const imgUrls = []
  const prices = []
  const appIds = []
  const discounts = []
  const descriptions = []

  function createCarouselItem() {

  }

  document.querySelectorAll('.recently_updated.carousel_items > div > a').forEach((value, _) => {
    gameUrls.push(value.getAttribute('href'))
    imgUrls.push(value.querySelector('.capsule > img').getAttribute('src'))
    prices.push(value.querySelector('div:not([class])').innerText)
    appIds.push(value.getAttribute('data-ds-appid'))
    discounts.push(value.querySelector('div:not([class]) > div').getAttribute('data-discount'))
    descriptions.push(value.querySelector('.recently_updated_desc').innerText)
  })*/
}

recentlyUpdated()
